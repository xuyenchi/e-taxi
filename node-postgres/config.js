var config = {};
config.USER = 'taxi';
config.HOST = 'localhost';
config.DATABASE = 'etaxiDB';
config.PASSWORD = '123456';
config.PORT = 5433;

config.RADIUS = 10000; // bán kính tìm kiếm tài xế taxi tính theo đơn vị m;
config.APP_PORT = 4000;

module.exports = config;